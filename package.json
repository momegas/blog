{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "start:prod": "lerna run start:prod --stream"
  }
}
